# Copy to .env for local netlify dev (do not commit .env).
# In production, set these in Netlify Site settings Environment variables.

TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Google Maps JavaScript API key (Places Autocomplete). Restrict key by HTTP referrer in Google Cloud.
GOOGLE_MAPS_API_KEY=
